name: moonje_mate
description: 두뇌플러의 문제메이트

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.3 <4.0.0'


dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  supabase_flutter: ^2.3.4 # supabase 라이브러리
  image_picker: ^1.0.7 # 이미지 데이터 가져오기 라이브러리
  intl: ^0.19.0 # DateFormat을 활용할 수 있는 라이브러리
  http: ^1.2.1 # http 라이브러리

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0


flutter:
  assets:
    - assets/
  uses-material-design: true


